{
  "name": "monkmail",
  "version": "1.0.5",
  "description": "monkmail",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "npx ts-node --transpile-only test/index.ts",
    "test:queue": "npx ts-node --transpile-only test/demo-queue.ts",
    "test:attachments": "npx ts-node --transpile-only test/demo-attachments.ts"
  },
  "keywords": [
    "smtp",
    "github",
    "webhook"
  ],
  "author": "Jay",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  },
  "dependencies": {}
}